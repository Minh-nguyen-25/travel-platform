# Frontend Services Directory
# Các service gọi API của từng chức năng sẽ đặt ở đây theo chuẩn: <feature>.service.ts
# Ví dụ: destination.service.ts, trip.service.ts, review.service.ts
# Tất cả service gọi qua `axiosClient` từ `@/api/axiosClient`.
